/**
 * LoginScreen — placeholder da fundação.
 * O login real (tutor/vet) entra em specs futuras; por ora a entrada no app se dá
 * pela RoleSelect (que chama o `login(role)` mockado).
 */
import { PlaceholderScreen } from '../PlaceholderScreen';

export function LoginScreen() {
  return <PlaceholderScreen title="Login" subtitle="Em construção" />;
}
